import { UserResponse } from "@/api/autogenerated-api"
import { normaliseApiError } from "@/api/errors"

export type User = {
  id: number
  username: string
  isActive: boolean
}

function mapUser(dto: UserResponse): User {
  return {
    id: dto.id,
    username: dto.username,
    isActive: dto.is_active,
  }
}

// TODO: Use factories.
